# grill-me

Interview the user one question at a time until each branch of the design tree is resolved.

Intentionally a single short page — resist adding sections, references, or scripts. Its value is that it fits in one read.

*Dev-time only — this file never ships. The shipped surface is `SKILL.md`; see the root `CLAUDE.md` "Shipped artefacts vs dev-time files".*
