---
description: Clone AL Guidelines mirror
---

# Setup AL Guidelines Mirror

Clone the AL Guidelines mirror to `_aldoc/alguidelines.dev`.

**Important:** Ask user to confirm before running. Only run if explicitly requested.

## Command
```powershell
pwsh -NoProfile -ExecutionPolicy Bypass -File "${CLAUDE_PLUGIN_ROOT}/skills/al-agentic-guidelines/scripts/setup-alguidelines-mirror.ps1"
```
