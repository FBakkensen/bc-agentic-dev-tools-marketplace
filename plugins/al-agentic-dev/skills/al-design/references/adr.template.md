# {Short title — the decision, not the topic}

{1–3 sentences. What was decided and the hinge of why. Hard cap. No restating the title. No future-roadmap sketches.}

---

Optional sections — gated, not free. Skip when not load-bearing.

**Considered Options** — only when a future reader will re-litigate the decision without seeing the alternatives. One bullet per option, ≤25 words, lead with the option then the reason it lost.

**Consequences** — only when a non-obvious downstream effect exists (typically a `/al-implement` discipline rule the ADR creates). Skip if it just paraphrases the lead.

**Status** — only when `superseded by ADR-NNNN`. Default state needs no line.
