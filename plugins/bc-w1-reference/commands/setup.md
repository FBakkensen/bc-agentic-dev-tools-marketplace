---
description: Clone BC W1 source mirror
---

# Setup BC W1 Mirror

Clone the Business Central W1 source code mirror to `_aldoc/bc-w1`.

**Important:** Ask user to confirm before running. Only run if explicitly requested.

## Command
```powershell
pwsh -NoProfile -ExecutionPolicy Bypass -File "${CLAUDE_PLUGIN_ROOT}/skills/bc-w1-reference/scripts/setup-bc-w1-mirror.ps1"
```
